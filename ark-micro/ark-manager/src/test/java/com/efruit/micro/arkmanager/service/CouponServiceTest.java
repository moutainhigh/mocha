package com.efruit.micro.arkmanager.service;

import com.efruit.micro.arkmanager.BaseTest;
import org.joda.time.DateTime;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;

public class CouponServiceTest extends BaseTest {

    @Autowired
    CouponService couponService;

    @Test
    public void testGetDelayTimeMillis() {
//        DateTime dateTime = new DateTime(2018, 10, 23, 16,14,0);
//        final long delayTimeMillis = couponService.getDelayTimeMillis(dateTime.toDate());
//        System.out.println("delayTimeMillis : " + delayTimeMillis);
    }
}
